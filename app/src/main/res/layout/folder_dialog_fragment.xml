<?xml version="1.0" encoding="utf-8"?><!--
  ~ Copyright © 2014-2026 The Android Password Store Authors. All Rights Reserved.
  ~ SPDX-License-Identifier: GPL-3.0-only
  -->

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:tools="http://schemas.android.com/tools"
  android:layout_width="match_parent"
  android:layout_height="wrap_content"
  android:orientation="vertical"
  android:padding="@dimen/activity_horizontal_margin">

  <com.google.android.material.textfield.TextInputLayout
    android:id="@+id/folder_name_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:hint="@string/crypto_name_hint"
    app:hintEnabled="true">

    <com.google.android.material.textfield.TextInputEditText
      android:id="@+id/folder_name_text"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:inputType="textNoSuggestions|textVisiblePassword" />
    <requestFocus />
  </com.google.android.material.textfield.TextInputLayout>

  <!--
    Named as a field rather than a line of text, because it is one: it holds a value, it can be
    changed, and it says which of the two outcomes will happen — the folder inherits the key of
    the folder above it, or overrides it. It is not typed into; tapping it opens the key picker.
  -->
  <com.google.android.material.textfield.TextInputLayout
    android:id="@+id/gpg_key_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="@dimen/spacing_medium"
    android:hint="@string/folder_encryption_key"
    android:visibility="gone"
    app:endIconContentDescription="@string/folder_encryption_key_choose"
    app:endIconDrawable="@drawable/ic_keyboard_arrow_right_24dp"
    app:endIconMode="custom"
    tools:visibility="visible">

    <com.google.android.material.textfield.TextInputEditText
      android:id="@+id/gpg_key_value"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:clickable="true"
      android:cursorVisible="false"
      android:ellipsize="middle"
      android:focusable="false"
      android:inputType="none"
      android:maxLines="1"
      android:text="@string/folder_encryption_key_inherited" />

  </com.google.android.material.textfield.TextInputLayout>

</LinearLayout>
