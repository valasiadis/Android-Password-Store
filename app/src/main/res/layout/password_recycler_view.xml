<?xml version="1.0" encoding="utf-8"?><!--
  ~ Copyright © 2014-2026 The Android Password Store Authors. All Rights Reserved.
  ~ SPDX-License-Identifier: GPL-3.0-only
  -->

<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:tools="http://schemas.android.com/tools"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:clipChildren="false"
  android:clipToPadding="false"
  android:orientation="vertical"
  tools:context="app.passwordstore.ui.passwords.PasswordFragment">

  <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
    android:id="@+id/swipe_refresher"
    android:enabled="false"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <me.zhanghai.android.fastscroll.FixOnItemTouchListenerRecyclerView
      android:id="@+id/pass_recycler"
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:clipToPadding="false"
      android:paddingTop="@dimen/spacing_xsmall"
      android:paddingBottom="@dimen/list_scroll_bottom_padding"
      android:scrollbars="none"
      tools:itemCount="20"
      tools:listitem="@layout/password_row_layout" />
  </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

  <!--
    Shown by screens that opt in, so navigating into a folder with nothing to show
    explains itself instead of presenting a blank list.
  -->
  <androidx.appcompat.widget.AppCompatTextView
    android:id="@+id/empty_message"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:paddingHorizontal="@dimen/spacing_xlarge"
    android:textAppearance="?attr/textAppearanceBodyLarge"
    android:textColor="?attr/colorOnSurfaceVariant"
    android:visibility="gone"
    tools:text="@string/folder_list_empty"
    tools:visibility="visible" />

  <!--
    The bottom of the screen is one piece, and travels as one: the keyboard pushes this row up by
    moving the row, not the three things in it, so they cannot arrive at different times or drift
    apart on the way. What they do among themselves — a button going, the field taking the room it
    leaves — is animated within the row, against neighbours that are standing still.
  -->
  <FrameLayout
    android:id="@+id/bottom_bar"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="bottom"
    android:clipChildren="false">

    <!--
      Sync sits on the leading edge, level with the creation button opposite it, rather than
      stacked above that button where the two read as one column of related actions.
    -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
      android:id="@+id/fab_sync"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_gravity="center_vertical|start"
      android:layout_margin="@dimen/fab_compat_margin"
      android:contentDescription="@string/password_list_fab_content_description"
      android:scaleType="center"
      android:src="@drawable/ic_cloud_sync_48dp"
      android:visibility="gone"
      app:maxImageSize="32dp" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
      android:id="@+id/fab"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_gravity="center_vertical|end"
      android:layout_margin="@dimen/fab_compat_margin"
      android:contentDescription="@string/password_list_fab_content_description"
      android:src="@drawable/ic_add_48dp" />

    <!--
      Searching is what this screen is mostly for, so it sits in reach of a thumb between the two
      buttons rather than behind a magnifier in the title bar at the other end of the screen.

      Laid out from the leading edge rather than centred: a centred child of this container is put
      at half the room it leaves and then shifted by the difference between its two margins, which
      for a field keeping a wide gap for one button and a narrow one for the other takes its
      leading edge off the side of the screen.
    -->
    <LinearLayout
      android:id="@+id/search_bar"
      android:layout_width="match_parent"
      android:layout_height="@dimen/search_bar_height"
      android:layout_gravity="center_vertical|start"
      android:layout_marginHorizontal="@dimen/search_bar_side_room"
      android:layout_marginVertical="@dimen/fab_compat_margin"
      android:background="@drawable/search_bar_background"
      android:gravity="center_vertical"
      android:orientation="horizontal"
      android:paddingHorizontal="@dimen/spacing_large">

      <androidx.appcompat.widget.AppCompatImageView
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:importantForAccessibility="no"
        android:src="@drawable/ic_search_24dp"
        app:tint="?attr/colorOnSurfaceVariant" />

      <androidx.appcompat.widget.AppCompatEditText
        android:id="@+id/search_input"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_marginStart="@dimen/spacing_medium"
        android:layout_weight="1"
        android:background="@null"
        android:hint="@string/action_search"
        android:imeOptions="actionSearch"
        android:inputType="textNoSuggestions"
        android:textAppearance="?attr/textAppearanceBodyLarge"
        android:textColor="?attr/colorOnSurface"
        android:textColorHint="?attr/colorOnSurfaceVariant" />

      <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/search_clear"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/search_clear"
        android:src="@drawable/ic_close_24dp"
        android:visibility="gone"
        app:tint="?attr/colorOnSurfaceVariant"
        tools:visibility="visible" />

    </LinearLayout>
  </FrameLayout>
</androidx.coordinatorlayout.widget.CoordinatorLayout>
