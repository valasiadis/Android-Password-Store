<?xml version="1.0" encoding="utf-8"?><!--
  ~ Copyright © 2014-2026 The Android Password Store Authors. All Rights Reserved.
  ~ SPDX-License-Identifier: GPL-3.0-only
  -->

<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:tools="http://schemas.android.com/tools"
  android:layout_width="match_parent"
  android:layout_height="@dimen/list_item_height"
  android:layout_marginHorizontal="@dimen/spacing_small"
  tools:background="@drawable/password_row_background">

  <!--
    No background here: a row's container is given to it by PasswordRowDecoration, which rounds
    it fully only where the list ends and slightly where rows meet, so the list reads as one
    block. The gap between rows comes from the same place, which is also why this layout has no
    vertical margin. The drawable above is the preview's stand-in for it.
  -->

  <androidx.appcompat.widget.AppCompatImageView
    android:id="@+id/type_icon"
    android:layout_width="24dp"
    android:layout_height="24dp"
    android:layout_marginStart="@dimen/spacing_medium"
    android:contentDescription="@null"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintTop_toTopOf="parent"
    app:tint="?attr/colorOnSurfaceVariant"
    tools:src="@drawable/ic_password_24px" />

  <!--
    The folder an entry came from, above its name. Its own view rather than a smaller span
    inside the name, because a shorter first line still reserves the taller line's metrics and
    left the pair sitting low in the row. The two are a packed chain, centred as one block.
  -->
  <androidx.appcompat.widget.AppCompatTextView
    android:id="@+id/parent_path"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_marginStart="@dimen/spacing_medium"
    android:ellipsize="middle"
    android:maxLines="1"
    android:textColor="?attr/colorOnSurfaceVariant"
    android:textSize="13sp"
    android:visibility="gone"
    app:layout_constraintBottom_toTopOf="@id/label"
    app:layout_constraintEnd_toStartOf="@id/child_count"
    app:layout_constraintStart_toEndOf="@id/type_icon"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintVertical_chainStyle="packed"
    tools:text="work/servers"
    tools:visibility="visible" />

  <androidx.appcompat.widget.AppCompatTextView
    android:id="@+id/label"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_marginStart="@dimen/spacing_medium"
    android:ellipsize="end"
    android:maxLines="1"
    android:textColor="?attr/colorPrimary"
    android:textSize="18sp"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintEnd_toStartOf="@id/child_count"
    app:layout_constraintStart_toEndOf="@id/type_icon"
    app:layout_constraintTop_toBottomOf="@id/parent_path"
    tools:text="FILE_NAME" />

  <androidx.appcompat.widget.AppCompatTextView
    android:id="@+id/child_count"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginStart="@dimen/spacing_small"
    android:layout_marginEnd="@dimen/spacing_small"
    android:textAppearance="?attr/textAppearanceLabelLarge"
    android:textColor="?attr/colorOnSurfaceVariant"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintEnd_toStartOf="@id/folder_indicator"
    app:layout_constraintTop_toTopOf="parent"
    tools:text="12" />

  <androidx.appcompat.widget.AppCompatImageView
    android:id="@+id/folder_indicator"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginEnd="@dimen/spacing_medium"
    android:contentDescription="@null"
    android:src="@drawable/ic_keyboard_arrow_right_24dp"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintTop_toTopOf="parent"
    app:tint="?attr/colorOnSurfaceVariant" />

</androidx.constraintlayout.widget.ConstraintLayout>
